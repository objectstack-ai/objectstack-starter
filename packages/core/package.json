{
  "name": "@objectstack-starter/core",
  "version": "0.1.0",
  "description": "ObjectStack Starter Template - Core package with base objects and views",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "dev": "tsc --build --watch",
    "clean": "tsc --build --clean && rm -rf dist",
    "type-check": "tsc --build --dry"
  },
  "dependencies": {
    "@objectstack/spec": "^0.3.3"
  },
  "devDependencies": {
    "@types/node": "^25.0.10",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "objectstack",
    "low-code",
    "metadata-driven",
    "core"
  ],
  "license": "MIT"
}
